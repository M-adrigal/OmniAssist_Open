{
  "name": "save_to_excel",
  "description": "将表格数据保存为Excel(.xlsx)文件。支持两种模式：\n1. 简单模式：仅提供headers、rows和filename，生成带基础样式的表格\n2. 排版模式：额外提供formatting参数（JSON对象），精确控制多Sheet、单元格样式、数字格式、条件格式、图表等细节\n\n当用户提出任何表格样式或排版要求时（如字体、颜色、边框、数字格式、条件格式、图表等），必须使用排版模式，提供formatting参数。",
  "parameters": {
    "type": "object",
    "properties": {
      "headers": {
        "type": "string",
        "description": "（简单模式）Excel表头，多个表头用逗号分隔，例如：'姓名,年龄,城市'。排版模式下可为空字符串"
      },
      "rows": {
        "type": "string",
        "description": "（简单模式）Excel数据行，每行用换行符分隔，行内各列用逗号分隔。例如：'张三,28,北京\\n李四,32,上海'。排版模式下可为空字符串"
      },
      "filename": {
        "type": "string",
        "description": "Excel文件名（无需扩展名），例如：'员工信息'、'销售报表'"
      },
      "formatting": {
        "type": "object",
        "description": "排版配置JSON对象。当用户有排版要求时提供，可控制多Sheet、单元格样式、数字格式、条件格式、图表等。不提供则使用简单模式",
        "properties": {
          "sheets": {
            "type": "array",
            "description": "工作表数组，每个元素描述一个Sheet，包含name、data、styles、conditional_formats、charts等"
          },
          "sheet_order": {
            "type": "array",
            "description": "Sheet排列顺序，字符串数组"
          }
        }
      }
    },
    "required": [
      "filename"
    ]
  },
  "execution_mode": "local_execution",
  "execution_prompt": "请编写Python代码，使用openpyxl库将表格数据保存为Excel文件。参数为：{params}。确保将文档保存到项目根目录下的'document_output/excel_output'文件夹中。如果文件夹不存在，请创建它。将生成的文档的完整文件路径赋值给变量result。只输出纯Python代码。",
  "execution_code": "import sys\nimport os\nimport json\nsys.path.insert(0, os.getcwd())\nfrom agent.excel_formatter import create_excel_workbook\n\noutput_dir = os.path.join('document_output', 'excel_output')\n\ntry:\n    formatting\nexcept NameError:\n    formatting = None\n\nfmt = None\nif formatting and isinstance(formatting, str):\n    try:\n        fmt = json.loads(formatting)\n    except:\n        pass\nelif formatting and isinstance(formatting, dict):\n    fmt = formatting\n\nresult = create_excel_workbook(filename, headers=headers or '', rows=rows or '', formatting=fmt, output_dir=output_dir)",
  "dependencies": [
    "openpyxl"
  ],
  "output_dir": "document_output/excel_output",
  "created_at": "2026-05-11T13:14:37.124841"
}